# Исправления развертывания Timeweb

## Проблемы которые были исправлены:

### 1. Nginx перезапускался из-за отсутствующих SSL сертификатов
**Проблема**: Nginx пытался использовать HTTPS конфигурацию даже в HTTP-only режиме
**Решение**: 
- Создана отдельная HTTP-only конфигурация (`nginx-timeweb/default-http.conf`)
- Deployment скрипт теперь выбирает правильную конфигурацию на основе SSL статуса

### 2. Certbot перезапускался в HTTP-only режиме
**Проблема**: Certbot пытался получить сертификаты даже когда SSL отключен
**Решение**: 
- Добавлен профиль `ssl` для certbot сервиса
- Certbot запускается только когда SSL включен

### 3. Health check пытался проверить внешние домены
**Проблема**: Health check скрипт пытался проверить внешние домены через nginx, который еще не был готов
**Решение**: 
- Создан `simple_healthcheck.py` для проверки только локального Django приложения
- Deployment скрипт использует простую проверку во время развертывания

### 4. Недостаточное время ожидания nginx
**Проблема**: Тесты endpoints запускались до готовности nginx
**Решение**: 
- Добавлено ожидание готовности nginx (до 60 секунд)
- Улучшена диагностика при неудачных тестах

## Файлы которые были изменены:

1. `docker-compose.timeweb.yml` - добавлен профиль для certbot, обновлена nginx конфигурация
2. `.github/workflows/deploy_timeweb.yml` - добавлена логика выбора nginx конфигурации
3. `nginx-timeweb/default-http.conf` - новая HTTP-only конфигурация
4. `simple_healthcheck.py` - простой health check для deployment
5. `Dockerfile` - добавлен simple_healthcheck.py

## Результат:

✅ **Web контейнер**: Работает стабильно
✅ **База данных**: Работает стабильно  
✅ **Health check**: Проходит успешно
🔧 **Nginx**: Должен теперь работать стабильно в HTTP-only режиме
🔧 **Certbot**: Не запускается в HTTP-only режиме (это правильно)

## Следующие шаги:

1. Запустить новое развертывание для проверки исправлений
2. Если nginx все еще перезапускается, проверить логи: `docker-compose -f docker-compose.timeweb.yml logs nginx`
3. После стабилизации HTTP режима можно работать над настройкой SSL сертификатов