# Раздел «Аналитика»: аудит и план переделки

Дата аудита: 2026-09-25. Источники: код в `main` (`d773c46`) и чтение боевой БД на сервере `tw`
(`docker exec insflow-system-web-1 python manage.py shell`, только SELECT). В рамках аудита код не менялся.

Документ — базовый для всей дальнейшей работы по разделу аналитики. Статус задач отмечается прямо здесь.

---

## 0. Выводы в двух словах

- Данных достаточно, чтобы ответить на главные вопросы владельца: 321 заявка (с 10.11.2025), 3 665 предложений,
  227 акцептованных сделок, 12 выбранных СК, журнал входов/действий (easy-audit, с 29.06.2026)
  и журнал смен статусов (`StatusEvent`, с 27.04.2026).
- Раздел построен вокруг вопроса «выбрал ли клиент самое дешёвое». Вопросы владельца — «куда уходит бизнес
  по страховым компаниям и почему» и «как реально работают сотрудники» — закрываются частично и косвенно.
- Страница сотрудников перегружена (66 карточек, радар, composite-score, heatmap’ы) и местами меряет не то:
  всё строится только по `request.created_by`; журнал easy-audit аналитика не использует вообще.
- Ключевых фактов нет в структурированном виде: отказы СК живут только в тексте примечаний,
  время ответа СК не фиксируется, по каким СК вообще был запрос — неизвестно.

## 1. Цели раздела (зафиксировано владельцем)

1. **Главная — страховые компании.** Куда уходит имущество, которое мы страхуем: какая СК, какой объём,
   что к ней пришло, почему (насколько это объяснимо данными). Развитие — отдельная страница-карточка на каждую СК.
2. **Сотрудники.** Как часто и как много заходят, что делают, какая нагрузка на отрезке (3 месяца, полгода),
   сколько реально занимаются задачами. Присутствие — косвенный, но полезный сигнал для контроля.
   «Кто чем занят прямо сейчас» — не приоритет.

## 2. Ответы владельца на открытые вопросы (2026-09-25)

- **Причина выбора СК — не собираем.** Сотрудники обрабатывают данные и не общаются с клиентом, решение всегда
  за клиентом, и его мотив неизвестен. Никаких полей или списков «почему выбрали» не делаем.
  «Почему» показываем только косвенно, по данным: цена, ранг, отставание от минимума, филиал, тип, объект.
- **Причина отказа СК — не собираем.** Страховые не объясняют отказ. Отказ — это просто отказ, без справочника причин.
- **Новая приоритетная идея — статус каждой СК в своде.** При работе со сводом сотрудник обязан по каждой
  страховой компании отметить её статус. Варианты:
  - **не запрашивалась**;
  - **запрошена** (ждём ответа);
  - **отказ**;
  - **предложение** — ставится автоматически, когда по СК загружено или введено предложение.
  Проработать отдельными задачами (см. §6, этап 2).
- **Страница сотрудников — и контроль, и нагрузка.** Нагрузку смотреть на отрезке (квартал, полугодие),
  а не «прямо сейчас». Присутствие — вспомогательный блок.
- Порядок работ и список того, что убрать или оставить, **одобрены полностью**.

---

## 3. Что есть сейчас (инвентаризация)

Всё под `@admin_required`, в меню «Аналитика» (`requires_admin`), маршруты — `summaries/urls.py`,
навигация — `onlineservice/context_processors.py` (`MAIN_NAV_ITEMS`, `SECTION_CONFIG['analytics']`,
`ADMIN_ONLY_ROUTES`, `SECTION_ROUTE_OVERRIDES`, заголовки, breadcrumbs, layout).

| Страница | URL / view | Код | Что внутри |
|---|---|---|---|
| Обзор аналитики | `analytics/` → `analytics_placeholder` | `analytics_placeholder.html` (129 стр.) | Только плитки-ссылки на 5 отчётов, ни одной цифры |
| Страховые предложения | `analytics/insurance-offers/` → `analytics_insurance_offers` | `views.py` 2733–2882, шаблон 401 стр. | По каждой акцептованной сделке: выбран ли минимум, ранг, разброс, «линейка» предложений. KPI: сделок, доля min, медиана Δ |
| Страховые компании | `analytics/insurance-companies/` (+ `/export/`) | `services/analytics_insurance_companies.py` (825), шаблон 1128 | 18 KPI, рейтинг, «конкурентность», «конверсия», 4 таблицы срезов (филиал / менеджер Альянса / тип / статус сделки), динамика по месяцам, data quality, 4 графика, XLSX |
| Сотрудники | `analytics/managers/` (+ `/export/`) | `services/analytics_managers.py` (2127), шаблон 1136 | 13 KPI, воронка, 6 таблиц (метрики, time-to, completeness, портфель, паттерны), heatmap день×час, WoW/MoM, радар, 4 heatmap «сотрудник × филиал/тип/менеджер Альянса/СК», 3 графика, алерты (просрочки, зависшие, без активности, падение объёма, выбросы цикла), backlog |
| Досье сотрудника | `analytics/managers/<id>/` (+ export) | шаблон 448 | Радар, лента действий, статус-события, алерты |
| Сравнение | `analytics/managers/compare/` | шаблон 227 | Радар и таблица side-by-side |
| «Леденборд» | `analytics/managers/leaderboard/` | шаблон 120 | Рейтинг по composite-индексу (опечатка в названии) |
| Правки парсера | `analytics/parser-edits/` | `services/analytics_parser_edits.py` (242) | Где парсер V2 ошибается (поле, формат, филиал, оператор, месяц) |
| Правки после создания | `analytics/post-creation/` | `services/analytics_post_creation.py` (193) | Какие поля меняют после создания, кто, подозрения на пропуск ошибки парсера |
| Статистика (раздел «Своды») | `statistics/` (+ export) | `views.py` 2059–2520, шаблон 823 | Старая версия: статусы, СК × тип, филиалы, пользователи по месяцам. Жёстко прописан порядок пользователей `['grigoriigrachev', 'test_user', 'testuser']` |

Тесты: `summaries/test_analytics_*.py` (10 файлов), `summaries/test_basic_integration.py`.
Описание метрик СК: `docs/analytics_insurance_companies_metrics.md`.

Ключевые детали реализации:
- Аналитика СК и «Страховые предложения» берут **только** `InsuranceSummary.status='completed_accepted'`
  с непустым `selected_company` (`build_analytics_insurance_companies_payload`, база queryset).
- Ценовое сравнение — `summaries.views._build_deal_price_row` (сумма премий по годам, вариант франшизы
  `selected_franchise_variant` с fallback на 1, опция «полное покрытие лет», режим `selected_variant`/`best_available`).
- Аналитика сотрудников: единица атрибуции — `InsuranceRequest.created_by`. Совокупный индекс
  `QUALITY_SCORE_WEIGHTS = completeness 0.40, win_rate 0.30, speed 0.20, volume 0.10`.
  Радар `RADAR_AXES` с английскими подписями (Volume, Speed, Win-rate, Quality, Money, Activity).
- easy-audit (`LoginEvent`, `CRUDEvent`, `RequestEvent`) в аналитике не используется.
  Retention: Login и CRUD — 90 дней, Request — 1 день (`purge_audit_log`, cron 04:00).

---

## 4. Что показывают реальные данные (прод, 2026-09-25)

### 4.1. Общий объём

- Заявки: 321 (10.11.2025 – 25.09.2026). Типы: КАСКО 214, спецтехника 80, имущество 25, другое 2.
  Новые 317, пролонгации 4.
- По месяцам: 11.25 — 20, 12.25 — 20, 01 — 13, 02 — 18, 03 — 37, 04 — 55, 05 — 27, 06 — 50, 07 — 27, 08 — 30, 09 — 24.
- Своды: 321; акцепт 227, «не будет» 81, отправлены 13. Отказ клиента ≈ 26% закрытых.
- Предложения: 3 665, все `is_valid=True`. Уникальных СК на свод — чаще всего 4–7 (медиана около 5).
- Филиалы (заявки): Краснодар 93, Москва 47, Псков 32, СПб 30, Мурманск 29, Челябинск 23, Архангельск 22,
  В. Новгород 21, Казань 17, Н. Новгород 7. Менеджеров Альянса (`manager_name`) — 45.
- V2-пакеты (несколько объектов в одном файле): 13 заявок.
- Справочник `InsuranceCompany`: 16 записей (Абсолют, Альфа, ВСК, Согаз, РЕСО, Ингосстрах, Ренессанс, Росгосстрах,
  Пари, Совкомбанк СК, Согласие, Энергогарант, ПСБ-страхование, Зетта, Югория, «другое»).

### 4.2. Страховые компании (227 акцептов, режим по умолчанию)

KPI страницы сейчас: сопоставимых сделок 181, выбран минимум в 69%, медиана Δ к минимуму 0 ₽,
средний ранг 1,59, в среднем 4,5 конкурента, многолетних 82%, с рассрочкой 7%,
в среднем 3,4 ч от заявки до свода, 190 ч от свода до закрытия.

| СК | Участвовала (в акцептах) | Выбрана | Доля выигрышей | Страховая сумма, 1-й год | Премия выбранных, все годы | Ранг, когда выбрана | Выбрана как минимум |
|---|---|---|---|---|---|---|---|
| Абсолют | 103 | 59 | 57% | ≈ 632 млн | 8,6 млн | 1,04 | 96% |
| Ингосстрах | 172 | 42 | 24% | ≈ 197 млн | 12,2 млн | 3,0 | 10% |
| Пари | 173 | 34 | 20% | ≈ 226 млн | 7,8 млн | 1,33 | 78% |
| Альфа | 130 | 29 | 22% | ≈ 170 млн | 7,0 млн | 1,76 | 56% |
| Росгосстрах | 36 | 16 | 44% | ≈ 88 млн | 3,7 млн | 1,18 | 82% |
| Зетта | 76 | 13 | 17% | ≈ 80 млн | 4,0 млн | 1,0 | 100% |
| Согласие | 63 | 13 | 21% | ≈ 139 млн | 2,5 млн | 1,36 | 64% |
| Согаз | 169 | 11 | 6,5% | ≈ 62 млн | 1,6 млн | 1,9 | 60% |
| Ренессанс | 90 | 5 | 5,6% | ≈ 32 млн | 1,1 млн | 1,25 | 75% |
| Югория | 5 | 2 | 40% | ≈ 10 млн | 0,8 млн | 3,5 | 0% |
| другое | 4 | 2 | 50% | ≈ 38 млн | 1,5 млн | 1,0 | 100% |
| ПСБ-страхование | 2 | 1 | 50% | ≈ 5 млн | ≈ 0 | — | — |
| ВСК | 106 | **0** | 0% | — | — | — | — |
| Энергогарант | 4 | 0 | 0% | — | — | — | — |
| РЕСО | 1 | 0 | 0% | — | — | — | — |

Итого по выбранным СК: страховая сумма 1-го года ≈ 1,68 млрд, премия ≈ 51 млн (все годы).

Профили выбранных СК:
- **Абсолют** — 22 КАСКО + 37 спецтехника. Москва 21, Псков 9, Челябинск 8, В. Новгород 7.
  Выбирают почти всегда как самую дешёвую. ≈ 38% всей застрахованной стоимости при 26% сделок.
- **Ингосстрах** — 36 КАСКО, 4 имущество, 2 спецтехника. **35 из 42 — Краснодар.**
  Выигрывает не ценой (ранг 3,0, минимум лишь в 10%). ≈ 24% премии при ≈ 12% страховой суммы — дорогая СК.
  Пики: апрель (18 выигрышей) и июнь (12).
- **Пари** — лидер по имуществу (13 из 25 заявок), плюс 15 КАСКО и 5 спецтехника. География широкая
  (СПб 8, Краснодар 7, Мурманск 6, Москва 4).
- **Альфа** — 26 КАСКО, 3 спецтехника. Краснодар 12, Мурманск 6, Челябинск 4.
- **Росгосстрах** — только КАСКО. В. Новгород 5, Псков 5, Москва 4.
- **Зетта** — только КАСКО, всегда как минимум. Псков 5, Москва 4.

Лидер по филиалам (акцепты): Москва — Абсолют 21 из 41; Краснодар — Ингосстрах 35 из 67;
Псков — Абсолют 9 из 21; В. Новгород — Абсолют 7 из 14; Челябинск — Абсолют 8 из 14;
СПб — Пари 8 из 26; Мурманск — Альфа 6 и Пари 6 из 19.

Участие по типам (своды с предложением): ВСК — 153 КАСКО и 2 спецтехники, ни одного выигрыша;
Согаз — 178 КАСКО, 67 спецтехника, 3 имущество, 11 выигрышей; Ингосстрах предлагает почти везде (249 сводов).

Отказы СК:
- Слово «отказ» встречается в `summary.notes` у **132 сводов** (87 акцепт, 37 «не будет», 8 отправлены).
  Формат свободный, например: «СК Абсолют, Югория, Зетта - ОТКАЗ», «Югория, Ренессанс - отказ от страхования
  (запрет по территории - Адыгея)», «ВСК - тариф не предоставлен…».
- Примечаний к предложениям (`offer.notes`) — 881.
- Структурированного признака отказа или «не запрашивали» нет.

### 4.3. Качество данных, влияющее на аналитику СК

- `selected_franchise_variant` пуст у **73 из 227** акцептов (32%) — сравнение идёт по варианту 1 по умолчанию.
  Все 73 закрыты до 01.04.2026; у 65 из них у выбранной СК был только один вариант (fallback корректен),
  реально неоднозначных — 8 (последний 02.03.2026). Правило `requires_variant_choice` сейчас работает, проблема историческая.
- Несопоставимых для ценового ранга сделок — 46 (20%).
- `completed_at` не проставлен у **всех 81** отказов клиента («не будет»). У акцептов он есть всегда.
  Длительность цикла отказов считается по `updated_at`.
- `sent_to_client_at` пуст у 20 сводов в статусах «отправлен» и дальше.
- `InsuranceOffer.received_at` — `auto_now_add`, то есть момент ввода оператором, а не время ответа СК.
  Скорость ответа страховых измерить нельзя.
- `acquisition_cost_value` заполнен у 120 заявок (только V2).

### 4.4. Сотрудники

Пользователи:

| Логин | Имя | Группа | Заявок создано | Дней со входом (с 29.06) | Последний вход |
|---|---|---|---|---|---|
| droZdova | Н.А. Дроздова | Пользователи | 186 | 48 | 25.09.2026 |
| lazArEva | Н.Н. Лазарева | Пользователи | 107 | 45 | 25.09.2026 |
| sergEEva | Т.Н. Сергеева | Пользователи | 28 | 51 | 24.09.2026 |
| goRovAya | Yana Gorovaya | Пользователи | 0 | 26 | 24.09.2026 |
| shiTushKina | Elena Shitushkina | Пользователи | 0 | 21 | 25.09.2026 |
| pAhoMova | Evgenia Pahomova | Пользователи | 0 | — | 23.04.2026 |
| insadmin | Flow Admin | Администраторы | — | 9 | 25.09.2026 |
| ifGrigra | — | (без группы) | — | — | 30.10.2025 |

- Горовая и Шитушкина — «читатели»: заходят регулярно, смотрят `/requests/`, карточки сводов и `deal-summary`.
  Текущая аналитика их не видит.
- Действия по CRUD-журналу, с 29.06.2026 (без записей `user`, то есть без обновлений last_login):

  | Сотрудник | Дней с действиями | Действий в день | Медианное окно дня | Часы активности |
  |---|---|---|---|---|
  | Дроздова | 45 | ≈ 27 | ≈ 3,9 ч | 9–17, ровно |
  | Лазарева | 31 | ≈ 37 | ≈ 2,4 ч | 9–17, плюс заметный пик в 22:00 |
  | Сергеева | 11 | ≈ 14 | ≈ 0,1 ч | 11–16, короткими пачками |

  По месяцам: Дроздова 06 — 122, 07 — 167, 08 — 562, 09 — 381; Лазарева 07 — 598, 08 — 124, 09 — 423;
  Сергеева 07 — 48, 08 — 109.
- Предложения практически всегда вносит владелец заявки (Дроздова 416/416, Лазарева 366 + 10 в заявки Дроздовой).
  То есть `created_by` ≈ исполнитель, но единственное поле атрибуции этого не гарантирует.
- Журнал статусов (с 27.04.2026), медианы:

  | Этап | Медиана | По сотрудникам |
  |---|---|---|
  | загружена → письма отправлены | 0,1 ч | Дроздова 0,2 / Лазарева 0,0 / Сергеева 0,2 |
  | свод «сбор» → «готов» | 3,8 ч | Дроздова 4,4 / Лазарева 3,7 / Сергеева 0,1 |
  | «готов» → «отправлен клиенту» | **0,0 ч** | статусы нажимают подряд |
  | «отправлен» → акцепт | 99 ч | время клиента, не сотрудника |

  Частые «откаты» статусов: sent→collecting 31, ready→collecting 15, rejected→accepted 6.
- Просмотры страниц (`RequestEvent`) хранятся 1 день: история «что смотрел и сколько был в системе» не накапливается.
  За сутки анонимный трафик — 4 722 запроса (сканеры: `/.env`, `/wp-login.php` и т. п.).

---

## 5. Оценка: что мешает и сбивает фокус

### 5.1. Методические проблемы

1. Аналитика СК строится только по акцептам. 81 отказ клиента и 13 сводов в работе не учитываются:
   «участие» занижено, воронки «запросили → ответила → отказала → предложила → выиграла» нет.
2. Страховая сумма (сколько имущества ушло в СК) на странице СК отсутствует, есть только сумма премий.
   Премия смешивает одно- и многолетние сделки.
3. Доля выигрышей и индекс качества приписываются сотруднику, хотя решение принимает клиент.
   40% индекса — «completeness» полей, которые в основном заполняет парсер из Excel.
4. Атрибуция сотрудников только по `created_by`. Внесение предложений, правки, просмотры и входы
   не учитываются, хотя easy-audit их пишет.
5. Метрики времени по статусам меряют клики, а не работу («готов → отправлен» = 0 ч).
   Полный цикл «загрузка → акцепт» в основном состоит из времени клиента.
6. Время ответа СК не измеряется (`received_at` = момент ввода).
7. Пустой `completed_at` у отказов клиента; исторически пустой `selected_franchise_variant` (8 неоднозначных сделок до марта 2026).
8. `RequestEvent` хранится 1 день, агрегата нет.

### 5.2. Лишнее

- Страница СК: 4 таблицы срезов (101 / 264 / 31 / 17 строк; «СК × менеджер Альянса» — 264 строки);
  таблицы «Конкурентность» и «Конверсия» повторяют рейтинг; блок data quality с техническими именами полей;
  три режима дат, режим сравнения, «полное покрытие лет» — настройки аналитика, а не руководителя.
- Сотрудники: composite quality-score, радар (англ. подписи), Леденборд, сравнение, 4 heatmap
  «сотрудник × измерение», completeness и портфельные флаги, доля выигрышей как метрика сотрудника.
- Дубли: «Страховые предложения» (подмножество страницы СК), старая «Статистика» в «Сводах».
- Правки парсера и правки после создания — технический контроль распознавания, а не бизнес-аналитика.

### 5.3. Что стоит сохранить

- Расчёт ценовой позиции сделки (`_build_deal_price_row`): ранг, Δ к минимуму, «выбран минимум».
  Это основа блока «почему выигрывает / проигрывает» в карточке СК.
- Алерты «зависшие своды» — перенести в блок нагрузки.
- Тепловая карта «день × час» — как часть блока присутствия, но по действиям из журнала, а не по загрузкам.
- XLSX-экспорт — на новых страницах, в упрощённом виде.
- Страницы правок парсера — переносятся в «Служебное», не удаляются.

---

## 6. Целевая структура и план работ

Порядок этапов одобрен владельцем. Внутри — задачи; каждая задача — отдельный коммит.
Push в `main` = деплой на прод, поэтому пушим только пакетом по команде владельца.

**Примечание о порядке.** Статусы СК в своде (этап 2) накапливают данные только с момента запуска и
задним числом не появляются (кроме частичного восстановления из примечаний, задача 2.5). Поэтому этап 2
имеет смысл запустить как можно раньше, параллельно с этапом 1 или сразу после.

### Этап 1. Расчистка и новый обзор

- [ ] **1.1. Удалить лишние страницы сотрудников.** Леденборд, сравнение, радар, composite quality-score
  (маршруты, views, шаблоны, context_processors, тесты `test_analytics_managers_phase*`).
  Досье пока оставить, оно будет переделано на этапе 4.
- [ ] **1.2. Убрать «Страховые предложения» как отдельную страницу.** Ценовой разбор сделки позже переедет
  в карточку СК (этап 3). До тех пор — редирект на страницу СК.
- [ ] **1.3. Убрать «Статистику» из «Сводов».** Маршрут редиректит на обзор аналитики.
  Удалить `_build_statistics_payload`, экспорт, шаблон и жёсткий порядок пользователей.
- [ ] **1.4. Страница СК, компактная версия («Куда уходит бизнес»).**
  - Период: быстрые 30 / 90 / 180 / 365 дней / всё / произвольный. Одна дата — создание свода.
    Режимы дат, `comparison_mode` и `full_coverage` убрать из UI; в коде оставить значения по умолчанию.
  - KPI: сделок (акцептов), страховая сумма 1-го года, премия, число СК-победителей, доля «выбран минимум».
  - Таблица СК: сделок / доля, страховая сумма / доля, премия / доля, средний тариф (премия 1-го года /
    страховая сумма), участие → выигрыши, доля выигрышей, «выбран как минимум».
  - Динамика долей СК по месяцам (stacked-график).
  - Матрицы «СК × филиал» и «СК × тип страхования» — тепловые карты вместо 4 длинных таблиц.
  - Убрать: таблицы «Конкурентность» и «Конверсия», срезы по менеджеру Альянса и по статусу сделки.
    Data quality свернуть под спойлер, подписи — по-русски.
- [ ] **1.5. Правки парсера и правки после создания → подраздел «Служебное».** Пункт в меню,
  с плитки на обзоре убрать.
- [ ] **1.6. Обзор аналитики с цифрами.** За выбранный период: заявки, акцепты, страховая сумма, премия,
  топ-3 СК по объёму, открытые своды. Два графика: заявки/акцепты по месяцам и доли СК.
  Ссылки на «Страховые компании», «Сотрудники», «Служебное».
- [ ] **1.7. Починить данные для аналитики.**
  - (a) проставлять `completed_at` при переходе в `completed_rejected`; бэкфилл по `StatusEvent`/`updated_at`;
  - (b) `selected_franchise_variant`: правило уже работает с апреля 2026. Опционально показать 8 исторических
    неоднозначных сделок владельцу для ручного проставления;
  - (c) проверить, почему `sent_to_client_at` пуст у 20 отправленных сводов.

### Этап 2. Статус каждой СК в своде (приоритетная идея владельца)

Цель — знать по каждому своду, какие СК запрошены, какие отказали, какие дали предложение, какие не запрашивались.
Это основа воронки СК и доли отказов в карточке компании.

Предлагаемая модель (финализировать перед реализацией):

- Новая таблица `SummaryCompanyStatus` (`summaries`): `summary` FK, `company` FK на `InsuranceCompany`
  (или `company_name`, как в `InsuranceOffer`, — решить при реализации), `status`,
  `status_changed_at`, `changed_by`, `unique_together = (summary, company)`.
- Статусы: `not_requested` «Не запрашивалась», `requested` «Запрошена», `declined` «Отказ»,
  `offered` «Предложение».
- `offered` выставляется автоматически, когда по СК сохраняется хотя бы одно предложение
  (ручной ввод, загрузка одного файла, пакетная загрузка). При удалении последнего предложения СК
  статус возвращается в `requested`.
- При создании свода строки создаются для всех активных СК справочника (кроме «другое») со статусом
  по умолчанию. Какой статус по умолчанию — открытый вопрос.
- UI: на карточке свода — компактная таблица СК с переключателем статуса (три кнопки) и отметкой «предложение»
  для СК с загруженными офферами.
- Обязательность: нельзя перевести свод в «готов» / «отправлен клиенту», пока у какой-либо СК статус
  не определён (или остаётся «запрошена»?) — открытый вопрос.

Задачи:

- [ ] **2.1. Модель и миграция.** Сигналы или сервис синхронизации статуса `offered` с `InsuranceOffer`.
- [ ] **2.2. UI на карточке свода.** Таблица статусов, массовые действия («все остальные — не запрашивались»).
- [ ] **2.3. Правило обязательности** при смене статуса свода. Сообщение, каких СК не хватает.
- [ ] **2.4. Отражение в журнале.** `StatusEvent` для статусов СК или отдельные поля времени; журнал для easy-audit.
- [ ] **2.5. (Опционально) Восстановить историю.** Для старых сводов: `offered` — по наличию предложений,
  `declined` — эвристикой по `summary.notes` (название СК рядом со словом «отказ»), остальное — «неизвестно».
  Сначала dry-run с отчётом для владельца.

Открытые вопросы к этапу 2:
1. Статус по умолчанию при создании свода: «не определён» (сотрудник обязан выбрать) или «запрошена» для всех?
2. На каком шаге обязательность: при переводе в «готов» или при отправке клиенту? Разрешена ли «запрошена»
   (СК не ответила) на момент отправки клиенту?
3. «Другое» — выводить ли в таблицу статусов?

### Этап 3. Карточка страховой компании

Страница `analytics/insurance-companies/<company>/`, ссылки из таблицы этапа 1.4.

- [ ] **3.1. Объём.** Сделок, страховая сумма, премия, средний тариф по типам; динамика по месяцам;
  доля СК в общем объёме.
- [ ] **3.2. Что к ней пришло.** Филиалы, типы страхования, виды и марки техники, новая или б/у,
  год выпуска, диапазоны стоимости (V2-поля `brand`, `equipment_type`, `condition`, `manufacturing_year`,
  `acquisition_cost_value`; для V1 — `object_summary`/`vehicle_info` как текст), крупнейшие лизингополучатели.
- [ ] **3.3. Почему выигрывает / проигрывает (по данным, без опроса).** Доля выигрышей ценой (выбрана как
  минимум) и не ценой; средний ранг и Δ к минимуму; где СК была самой дешёвой, но проиграла;
  разрез по филиалам (пример: Ингосстрах в Краснодаре). Ценовой разбор сделок из бывшей страницы
  «Страховые предложения».
- [ ] **3.4. Воронка (после этапа 2).** Запрошена → предложение / отказ / нет ответа → выигрыш.
  Доля отказов по типам, филиалам, объектам.
- [ ] **3.5. Список сделок** СК со ссылками на своды; XLSX-экспорт карточки.

### Этап 4. Сотрудники — пересборка

Одна страница «Сотрудники» с периодом (по умолчанию 90 дней; быстрые 90 / 180 / 365) и досье сотрудника
в той же логике. Учитываются все пользователи группы «Пользователи», включая тех, кто только смотрит.

- [ ] **4.1. Дневной агрегат активности.** Модель `UserDailyActivity` (user, date, число запросов, первый и последний
  запрос, активные минуты по сессиям с разрывом ≤ 20–30 мин, топ разделов, число CRUD по моделям).
  Наполняется из `RequestEvent` и `CRUDEvent` командой перед ночной чисткой (`purge_audit_log`, 04:00),
  чтобы сырые события по-прежнему хранились 1 / 90 дней.
- [ ] **4.2. Блок «Нагрузка за период».** По сотруднику и по неделям/месяцам: загружено заявок (и объектов),
  внесено предложений, собрано и отправлено сводов, закрыто сделок; доля от команды; средняя нагрузка
  в неделю; сравнение с предыдущим периодом.
- [ ] **4.3. Блок «Присутствие».** Дни со входом, дни с действиями, первое/последнее действие, активное время,
  тепловая карта «день × час» по действиям, поздние и выходные.
- [ ] **4.4. Блок «Скорость на своих этапах».** Загрузка → письма отправлены; последнее предложение →
  свод отправлен клиенту. Время клиента («отправлен → закрыт») — отдельно и без оценки сотрудника.
  Зависшие своды — список.
- [ ] **4.5. Удалить старое.** Composite-score, доля выигрышей как метрика сотрудника, heatmap «сотрудник ×
  филиал / тип / менеджер Альянса / СК», completeness, портфельные флаги, WoW/MoM-блок, лишние алерты.
  Досье переделать на блоки 4.2–4.4 плюс лента действий.

---

## 7. Журнал изменений плана

- 2026-09-25 — аудит, план, ответы владельца (§2).
