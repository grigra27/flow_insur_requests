# Implementation roadmap: модель → парсер → splitting

Дата: 2026-05-18.
Состояние: набросок для обсуждения. Каждый этап разберём детально перед стартом.

Документ описывает конкретные шаги реализации после уже сделанного **Этапа 1**
([поля партии в `InsuranceRequest`](../../insurance_requests/migrations/0032_add_batch_fields.py)).
Связанные документы: [own_insurance_request_form.md](own_insurance_request_form.md),
[coverage_audit.md](coverage_audit.md).

## Общая картина зависимостей

```
Этап 2 (модель)
   ├─ 2.1 Поля объекта             ✅
   ├─ 2.2 Реквизиты страхователя   ✅
   ├─ 2.3 Параметры сделки/страх.  ✅  ──> Этап 3 (парсер) ──> Этап 4 (splitting view)
   └─ 2.4 Структурированные блоки  ❌ (отменён по итогам аудита)
```

- **Этап 4 (splitting) разблокируется минимально после 2.1.** Этого
  достаточно, чтобы сёстры партии различались.
- **Этап 3 (парсер) тянет за собой все 2.x по мере готовности полей.**
  Можно расширять парсер инкрементально: чем больше полей в модели —
  тем больше парсер начинает заполнять.
- Каждый под-этап — отдельная миграция, отдельный коммит, отдельные тесты.

---

## Этап 2: расширение модели `InsuranceRequest`

Общие принципы для всех под-этапов:

- Все новые поля **`null=True, blank=True`** (см.
  [принцип совместимости](own_insurance_request_form.md#совместимость-парсеров-v1-и-v2)).
- Никаких `NOT NULL` и никаких дефолтных строк типа `'не указано'`.
- Поля хранятся **в `InsuranceRequest`**, отдельные таблицы не создаются
  (см. [архитектурный принцип «1 объект = 1 заявка»](own_insurance_request_form.md#ключевой-архитектурный-принцип-один-объект--одна-заявка)).
- Каждый под-этап после миграции добавляет smoke-тесты: создание записи
  со всеми полями, проверка `choices`, проверка дефолта `null`.
- V1 не трогаем; новые поля заполняет только V2 (на Этапе 3) или
  оператор руками через UI.

### Под-этап 2.1 — Поля объекта

**Цель.** Каждая заявка-сестра партии может различаться по объекту:
марка, модель, VIN, год, состояние, стоимость и т.п. Без этого
splitting на Этапе 4 бессмысленен — N сёстер будут полными клонами.

**Поля.**

| Поле                          | Тип                                                                 | Источник в JSON Schema v1               |
|-------------------------------|---------------------------------------------------------------------|------------------------------------------|
| `brand`                       | `CharField(max_length=128)`                                         | `insured_object.brand`                   |
| `model`                       | `CharField(max_length=255)`                                         | `insured_object.model`                   |
| `condition`                   | `CharField(max_length=10, choices=['new','used'])` (nullable)       | `insured_object.condition`               |
| `equipment_type`              | `CharField(max_length=128)`                                         | `insured_object.equipment_type`          |
| `power_or_capacity`           | `CharField(max_length=64)`                                          | `insured_object.power_or_capacity`       |
| `acquisition_cost_value`      | `DecimalField(max_digits=14, decimal_places=2)`                     | `insured_object.acquisition_cost.value`  |
| `acquisition_cost_currency`   | `CharField(max_length=3, choices=['RUB','USD','EUR'])`              | `insured_object.acquisition_cost.currency` |

Поля `vin`, `serial_number`, `quantity` из исходного плана **удалены** — они системно отсутствуют в источнике (см. [json_schema_v2.md](json_schema_v2.md)). Изначально были добавлены миграцией `0033`, удалены миграцией `0034_remove_vin_serial_quantity`.

**Что разблокирует.** Этап 4 (splitting). Парсинг строки объекта на
Этапе 3.

**Что отсюда НЕ убираем.** Существующие текстовые поля
(`manufacturing_year` как `CharField`, `asset_status`, `vehicle_info`)
остаются для совместимости. V2 пишет в них **и** в новые поля
(дублирование). После полного перехода на V2 — старые поля можно
deprecate'нуть отдельно.

**Решения по открытым вопросам.**

1. **`condition`**: nullable `CharField(choices=[('new','Новое'),('used','Б/у')])`. `null` означает «не определено». Без явного `unknown`-значения.
2. **`manufacturing_year_int` не добавляем.** Оставляем только существующий CharField `manufacturing_year`. Год для фильтров/сравнений извлекаем regex'ом из строки на ходу. Без миграции для года.
3. **`acquisition_cost_currency`**: closed `choices=[('RUB','Рубли'),('USD','Доллары США'),('EUR','Евро')]`. Парсер нормализует `руб`/`рублей`/`₽`→`RUB`, `$`/`долл`→`USD`.
4. **`vehicle_info` оставляем** — оба парсера пишут слиплённой строкой как раньше. Шаблоны и письма продолжают читать. Deprecate отдельной задачей после полного перехода на V2.

### Под-этап 2.2 — Реквизиты страхователя

**Цель.** Хранить реквизиты, которые есть в Excel, но никуда не
попадают: юр./почтовый адрес, вид деятельности, ОГРН/КПП, дата рождения
ИП, дата подачи заявки.

**Поля.**

| Поле                | Тип                                       | Источник                                  |
|---------------------|--------------------------------------------|--------------------------------------------|
| `legal_address`     | `TextField` (nullable)                    | `customer.legal_address` (30/30 в мини-аудите) |
| `postal_address`    | `TextField` (nullable)                    | `customer.postal_address` (30/30)         |
| `business_activity` | `TextField` (nullable)                    | `customer.business_activity` (30/30)      |
| `birth_date`        | `DateField` (nullable, для ИП)            | `customer.birth_date` (6/30)              |
| `submission_date`   | `DateField` (nullable)                    | `request.submission_date` (30/30)         |

Поля `ogrn` и `kpp` **исключены из плана** — мини-аудит 30 файлов
показал 0 hits. В исходном Excel заявки от лизинга реквизиты ФНС
страхователя не указываются. См. [json_schema_v2.md](json_schema_v2.md).

**Решения по открытым вопросам.**

1. **ОГРН/КПП**: не добавляем — отсутствуют в источнике.
2. **Адреса** хранятся в `TextField` без лимита — реальные адреса длинные
   и могут содержать переносы строк.
3. **Имя поля даты подачи** — `submission_date`, как в JSON Schema.

### Под-этап 2.3 — Параметры сделки и страхования

**Цель.** Структурировать параметры сделки и страхования, которые есть
в Excel заявки. Перед миграцией прогнан мини-аудит на 30 файлах
(`scripts/inspect_deal_fields.py`) — он сократил план с 11 полей до 5.

**Поля (финальный состав).**

| Поле                                | Тип                                                                                  | Hits в аудите |
|-------------------------------------|--------------------------------------------------------------------------------------|----------------|
| `insured_party`                     | `CharField(choices=[('lessor','Лизингодатель'),('lessee','Лизингополучатель'),('both','Оба')])`, nullable | 30/30      |
| `insured_sum_type`                  | `CharField(choices=[('aggregate','Агрегатная'),('non_aggregate','Неагрегатная')])`, nullable | 30/30  |
| `guard_conditions`                  | `TextField`, nullable — свободный текст («без ограничений», «гараж», ...)              | 29/30          |
| `property_location_right_holder`    | `CharField(choices=[('lessee_owner','Собственность лизингополучателя'),('third_party_owner','Стороннее лицо')])`, nullable. Для страхования имущества. | 1/30 (только property) |
| `premium_frequency`                 | `CharField(choices=[('single','Единовременно'),('quarterly','Поквартально'),('annual','Ежегодно')])`, nullable | 60/60   |

`premium_frequency` имеет только три значения, потому что в корпусе встречаются ровно три: `Единовременно`, `ежеквартально`, `ежегодно`. `semiannual` и `custom` исключены — если появятся, добавим миграцией.

**Поля, исключённые из плана (мини-аудит):**

- `contract_start_date` / `contract_end_date` — в Excel **нет конкретных дат** лизинга, только enum «на весь срок лизинга» (уже хранится в `insurance_period`).
- `period_start_date` / `period_end_date` / `period_months` — то же для периода страхования.
- `indemnity_basis` (с износом/без) — **0/30 hits**. В Excel заявки от лизинга не указывается.

**Решения по открытым вопросам.**

1. **Старые поля `insurance_period` и `has_installment` остаются.** Новые `premium_frequency` дополняет `has_installment` детализацией.
2. **Enum'ы — nullable без `unknown`.** Тот же паттерн, что в 2.1 для `condition`. NULL = «не определено».
3. **`property_location_right_holder`** живёт в той же модели, для casco-заявок просто остаётся NULL. UI/PDF сам решит, что показывать.
4. **`premium_frequency_comment` не добавляем** — список из 3 значений покрывает 100% корпуса.

### Под-этап 2.4 — Структурированные блоки **(ОТМЕНЁН)**

Этап 2.4 отменён по итогам мини-аудита
(`scripts/inspect_advanced_blocks.py` + `scripts/inspect_franchise_antitheft.py`,
30 + 5 файлов). Ни один из четырёх запланированных блоков нечем
заполнить из источника — лизинговая Excel-заявка их не содержит.

**Что обнаружено.**

| Блок                            | Реальность в Excel                                                                          | Решение                  |
|---------------------------------|---------------------------------------------------------------------------------------------|--------------------------|
| `franchise_options[]` (значения % / абс.сумм) | В R28 только заголовки колонок (`Нет франшизы` / `% от страховой суммы` / `Абсолютная сумма`), в R29 — отметка «Х» в одной из колонок. **Конкретных значений % или абсолютных сумм нет.** | Не добавляем             |
| `anti_theft` (alarm/immobilizer/mechanical/satellite) | Шаблонная таблица R51..R60 присутствует во всех 30 файлах, но **значения (марка, модель, конфигурация) пустые** в 5/5 проверенных детально. | Не добавляем             |
| `transportation_origin` / `destination` / `estimated_days` | Слово «перевоз» встречается в 7/30 файлах, но никаких origin/destination/маршрут не указано (0/30 with route hint). | Не добавляем             |
| `construction_work_description` (СМР)                    | 0/30 в корпусе. Согласуется с `has_construction_work = 0%` из основного аудита. | Не добавляем             |

То, что у нас уже есть, остаётся:

- Тип франшизы (enum `franchise_type` в `InsuranceRequest` + сырая
  структура в `additional_data['franchise_details']`) — этого достаточно
  для извлекаемого из источника.
- Boolean'ы `has_transportation` и `has_construction_work` — отражают
  сам факт необходимости услуги, без деталей.

Если в будущем страховщики потребуют детали этих блоков — оператор
сможет вводить их вручную, и тогда добавим соответствующие поля. Но
**добавлять колонки наперёд под несуществующий источник — отказываемся.**

См. также раздел в [json_schema_v2.md](json_schema_v2.md) о том, что
эти блоки не идут и в схему v2.

---

## Этап 3: парсер V2 — заполнение новых полей

**Цель.** V2 должен извлечь из Excel максимум того, что добавлено в
модель на Этапе 2.

### 3.1 — Разбор строки объекта (зависит от 2.1)

Сейчас V2 хранит объект как сырую строку:

```python
{"description": "LADA Largus KS045L 2024 б/у 78.05 1490000 руб", "year": "2024", "source": "..."}
```

Нужно разобрать на:

- `brand` / `model` — из начала строки до года.
- `vin` — поиск 17-символьного буквенно-цифрового кода (`[A-HJ-NPR-Z0-9]{17}` без I/O/Q).
- `serial_number` — для спецтехники (когда нет VIN).
- `manufacturing_year` (int) — уже извлекается, остаётся.
- `condition` — нормализовать «новое/новый/новая» → `new`, «б/у/бу/б-у» → `used`.
- `acquisition_cost.value` + `currency` — из столбцов M/N. По аудиту:
  M41 = `'Стоимость на момент приобретения'`, M43 = `'1490000'`, N43 = `'руб'`. То есть для парсера: ячейка M в строке объекта = value, N в той же строке = currency (нормализованная: `руб`→`RUB`).
- `equipment_type`, `power_or_capacity`, `quantity` — из соседних столбцов (L «Мощность двигателя» / L «колесная» / и т.п.).

**Открытые вопросы.**

1. Парсер строки объекта — regex-набор или попытка через NER/LLM? Я предлагаю regex+правила, LLM как fallback **только** для нераспознанных кейсов.
2. Если `brand`/`model` не удалось разделить, что сохранять? Целиком в `brand` и `model` оставлять null? Или наоборот? Я предлагаю: первое слово → `brand`, остальное до года → `model`; если разбить не удалось — всё в `model`.
3. Currency: список нормализаций — `руб` / `руб.` / `RUB` / `рублей` → `RUB`; `USD` / `$` / `долл` → `USD`. Согласовать.

### 3.2 — Реквизиты страхователя ✅

**Сделано.** V2 теперь label-based извлекает 5 полей страхователя
из шапки заявки. Fill rate на 178 файлах:

- `legal_address` — 92.1% (R8C2 «Юридический адрес:» → D8)
- `postal_address` — 55.6% (R10C2 «Почтовый адрес:» → D10)
- `business_activity` — 89.3% (R11C2 «Основной вид деятельности:» → D11)
- `birth_date` — 11.2% (только ИП-файлы)
- `submission_date` — 98.3% (L2 «дата подачи» → M2)

`parse_date_value` поддерживает форматы `%d.%m.%Y` / `%d.%m.%y`
(двузначный год для ИП) / `%Y-%m-%d` / ISO timestamp.

ОГРН/КПП отменены в этапе 2.2 — в источнике их нет.

### 3.3 — Параметры сделки и страхования ✅

**Сделано.** V2 заполняет 5 полей сделки и страхования. Fill rate
на 178 файлах:

- `insured_party` — 98.3% (R14C2 «Страхователь» → нормализация
  «ЛизингоДАТЕЛЬ» / «ЛизингоПОЛУЧАТЕЛЬ» в `lessor` / `lessee`)
- `insured_sum_type` — 98.3% (R24C4 «страховая сумма» → нормализация
  «Неагрегатная» / «Агрегатная» в `non_aggregate` / `aggregate`)
- `guard_conditions` — 91.6% (R26C4 «условия по охране» → свободный
  текст справа)
- `property_location_right_holder` — 6.7% (только property; R24C9
  «Правообладатель места расположения» → `lessee_owner` /
  `third_party_owner`)
- `premium_frequency` — 97.8% (отдельный метод
  `_extract_premium_frequency` ищет блок «Порядок уплаты страховой
  премии» и читает отмеченный «Х» вариант из строк R32-R35)

Даты лизинга / периода страхования и `indemnity_basis` отменены в
этапе 2.3 — в источнике их нет.

### 3.4 — Структурированные блоки **(ОТМЕНЁН)**

Зависел от Этапа 2.4, который отменён. Источника для конкретных
значений франшизы (%, абс.суммы), полей anti-theft (марка/модель
устройств), маршрутов перевозки и описания СМР в Excel заявки нет —
парсить нечего.

V2 продолжает извлекать то, что есть: тип франшизы (`franchise_type`)
и факт наличия перевозки/СМР (`has_transportation` / `has_construction_work`).
Этого достаточно.

---

## Этап 4: splitting во view'е `/upload-v2/`

**Цель.** Когда парсер V2 находит N объектов в файле, создаётся
N записей `InsuranceRequest` — по одной на объект.

### Под-этап 4.1 — Бэкенд splitting + read-only превью ✅

**Сделано.**

- `ParserV2PreviewForm` расширена 10 новыми общими полями 2.2/2.3
  (legal/postal/business/birth/submission + insured_party/sum_type/
  guard_conditions/property_location_right_holder/premium_frequency).
- `_create_requests_with_splitting()` в `views.py` создаёт записи
  в `transaction.atomic()`:
  - 0 объектов → 1 запись из общих полей формы (fallback).
  - 1 объект → 1 запись с объектными полями, **без** `source_batch_id`.
  - N ≥ 2 → N записей с общим `source_batch_id` (UUID), `item_no=1..N`,
    `item_count=N`. Общие поля дублируются.
- `_attach_parser_v2_original_file` обновлён: `cleanup_source` флаг
  позволяет attach к каждой сестре, удаление storage один раз в конце.
- Превью показывает read-only таблицу объектов партии (марка/модель/год/
  состояние/стоимость/описание), submit-кнопка адаптирует подпись
  («Создать партию из N заявок» vs «Создать заявку»).
- Тесты: 3 объекта → 3 сестры с общим batch_id, 1 объект → запись без
  batch_id, attachment к каждой.

### Под-этап 4.2 — Formset + UI редактирования объектов ✅

**Сделано.**

- `ParserV2ObjectForm` + `ParserV2ObjectFormSet` (formset_factory с
  `extra=0`) в `forms.py`. Каждая карточка: skip-чекбокс + поля объекта
  (brand/model/condition/equipment_type/power_or_capacity/cost+currency/
  manufacturing_year/vehicle_info). `to_object_kwargs()` возвращает
  готовые kwargs для `InsuranceRequest`.
- `parser_v2_object_initial_from_payload()` строит initial из payload.
- `_render_parser_v2_preview` принимает и рендерит formset с префиксом
  `objects-`.
- Confirm-step валидирует formset вместе с общей формой; собирает
  `object_kwargs_list` из cleaned_data, фильтрует skipped. Если все
  объекты отмечены к пропуску — ошибка «нечего создавать», превью
  рендерится повторно.
- `_create_requests_with_splitting()` теперь работает не с payload,
  а с готовым `object_kwargs_list` — payload используется только для
  инициализации formset.
- В шаблоне `upload_excel_v2_preview.html` секция объектов заменена
  на formset карточек; legacy-поля общей формы (`vehicle_info`,
  `manufacturing_year`, `asset_status`) скрываются, когда есть
  объектные карточки, чтобы избежать двойного ввода.
- Тесты:
  - Skip subset (3 объекта → отметили 1 → создаётся 2 сестры с
    `item_count=2`).
  - Skip all (3 объекта → все skip → 0 заявок, ошибка в response).
  - Edit propagation (правка `brand` и `acquisition_cost_value`
    отдельной карточки доходит до записи в БД).

### Под-этап 4.3 — Группировка партии в request_list ✅

**Сделано.**

- В `request_list` queryset: `order_by('-created_at', 'source_batch_id', 'item_no')` — сёстры партии идут подряд по `item_no`.
- В `_create_requests_with_splitting()` сёстры партии получают **общий `created_at`** через `update()` после создания. Без этого `auto_now_add` выдавал разные timestamps в цикле, и сёстры разбегались в списке.
- В шаблоне `request_list.html`:
  - У строк партии CSS-класс `request-row--batch` (синяя левая граница).
  - У первой сестры дополнительно `request-row--batch-first` (верхняя граница, отделяет группу сверху).
  - В колонке имени — badge «K/N» c tooltip «Партия из N заявок».
  - Стиль вынесен в блок `extra_css`.
- Тесты:
  - Сёстры идут подряд по `item_no` в queryset.
  - Badge «1/3», «2/3», «3/3» рендерится для каждой сестры.
  - Одиночная заявка (V1 или V2 N=1) не получает badge и не имеет batch CSS-класса.

### Логика

1. **Парсинг** — `ExcelRequestParserV2.parse()` возвращает payload с
   массивом объектов (как сейчас) + новые объектные поля (после Этапа 3).
2. **Превью** — оператор видит **список из N будущих заявок**, может:
   - отредактировать любую (например, исправить распознанную марку);
   - отбраковать любую (например, если в Excel объект-дубль);
   - подтвердить всю партию или отдельные заявки.
3. **Создание** — на confirm:
   - генерируется один `source_batch_id` (UUID);
   - создаётся `N` записей `InsuranceRequest` с общими реквизитами
     (клиент, ИНН, тип/период, франшиза, ДФА, филиал, менеджер,
     даты лизинга, …);
   - в каждой записи уникальны только **объектные поля** (Этап 2.1)
     и индексы партии (`source_batch_id`, `item_no`, `item_count`).
4. **Attachment** — оригинальный Excel прикрепляется **к первой
   заявке партии** (или ко всем? см. открытый вопрос ниже).

### UI-изменения

- Шаблон превью `upload_excel_v2_preview.html` сейчас рендерит одну
  форму. Нужно превратить в **список форм с общими и индивидуальными
  полями**.
- В списке заявок (`request_list`) добавить визуальную группировку
  по `source_batch_id`: «партия из 3 заявок» / отображение сёстер
  рядом.
- В детальной странице заявки (`request_detail`) показывать
  «В этой партии ещё N заявок» со ссылками.

### Открытые вопросы

1. **Attachment**: один файл (оригинальный Excel) прикрепляется
   к одной заявке или копируется во все N? Один — экономит место,
   но непонятно, к какой именно «вешать». Я предлагаю **прикрепить
   ко всем** (через `RequestAttachment` — это разные строки, не
   дублирование файла на ФС, если использовать одинаковый
   `file.name`).
2. **Превью при N>5**: показывать сразу все или с пагинацией?
   В корпусе максимум 15, обычно 2–6.
3. **Пакетные действия в превью**: «применить ИНН ко всем» /
   «применить тип страхования ко всем». Полезно при ручном
   редактировании, но усложняет UI. Можно отложить.
4. **Что если оператор отбраковал все заявки в превью?**
   Сейчас V2-флоу создаёт хотя бы одну запись. После splitting'а
   возможен кейс «0 заявок» — отдавать ошибку или просто отменять
   загрузку?
5. **Письмо страховщику**: подтверждаю принцип «1 заявка → 1 письмо»,
   но в UI отправка идёт батчем (выбрать партию → отправить N писем).
   Это уже Этап 5+, но дизайн UX надо держать в голове.

---

## Между Этапом 4 и Этапом 5 — JSON Schema v2

Сразу после splitting'а и **перед** генерацией собственного PDF — переход
на [JSON Schema v2](json_schema_v2.md). Что делаем:

- Создать `docs/insurance_request_format_package/insurance_request_schema_v2.json`:
  `insured_object` (singular, не массив), `customer.ogrn` / `customer.kpp`,
  `request.batch.*`, bump `schema_version` → `alliance.insurance_request.v2`.
- Удалить v1-схему и пересоздать примеры.
- Зафиксировать v2 как внутренний контракт между парсером, БД и
  будущим PDF/JSON-генератором.

## После Этапа 4

Дальше — собственный PDF и JSON-companion (Этап 5+, не входит в этот
roadmap; описан в [own_insurance_request_form.md](own_insurance_request_form.md#3-исходящая-заявка--наш-документ)).
PDF/JSON генерируются **на одну заявку**, как и письмо.
