# Timeweb Environment Variables for Landing Page Deployment
SECRET_KEY=your-timeweb-django-secret-key-minimum-50-characters-long
DB_NAME=insflow_db
DB_USER=insflow_user
DB_PASSWORD=secure-timeweb-database-password
DB_HOST=db
DB_PORT=5432
ALLOWED_HOSTS=insflow.tw1.su,zs.insflow.tw1.su,80.90.189.37
DOCKER_IMAGE=ghcr.io/grigra27/flow_insur_requests:timeweb-latest

# Security settings for production (HTTP only)
DEBUG=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Domain-specific settings for landing page
MAIN_DOMAIN=insflow.tw1.su
SUBDOMAIN=zs.insflow.tw1.su

# Static files configuration for both domains
STATIC_URL=/static/
MEDIA_URL=/media/

# Database engine for production
DB_ENGINE=django.db.backends.postgresql

# HTTP-only security settings
SECURE_SSL_REDIRECT=False